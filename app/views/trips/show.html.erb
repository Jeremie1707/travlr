
<div class="d-flex justify-content-start">
  <div class="ml-3">
    <h1 class="mb-0"><%= @trip.description %></h1>
    <p><%= @trip.description %></p>
  </div>
  <div class="d-flex mt-2 ml-2" id="date">
    <p><%= @trip.start_date %> </p>
    <p> &nbsp; to &nbsp; </p>
    <p><%= @trip.end_date %></p>
  </div>
  <div id="avatars" class="ml-3 m-2">
    <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", class: "avatar" %>
    <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", class: "avatar" %>
    <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", class: "avatar" %>
    <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", class: "avatar" %>

  <div class="buttonContainer">
    <a  href="#" class="tab-button" data-tab-index="0" data-color="#f44336">Tab 1</a>
    <a href="#" class="tab-button" data-tab-index="1" data-color="#f44336">Tab 2</a>
    <a href="#" class="tab-button" data-tab-index="2" data-color="#f44336">Tab 3</a>
    <a href="#" class="tab-button" data-tab-index="3" data-color="#f44336">Tab 4</a>
  </div>

  <div class="tabPanel">
    <div class="detail item container">
      <div class="container pt-5">
      <% @trip.trip_items.each do |element| %>
        <div class="trip-item-card">
        <%#= link_to trip_trip_item_path(element) do%>
          <%= element.name %>
          <p> 1.trip item</p>
          <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#card-trip-item-<%=element.id%>">
            TEST
          </button>
      <%# end %>
        </div>
      <% end %>
     </div>
    <% @trip.trip_items.each do |element| %>
    <%= render partial: "trip-item", locals: { element: element } %>
    <% end %>
</div>
</div>
</div>
  <div class="tabPanel">tab 2</div>
  <div class="tabPanel">
    <div
      id="map"
      style="width: 100%;
      height: 600px;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
     ></div>
    </div>
  <div class="tabPanel">tab 4</div>

