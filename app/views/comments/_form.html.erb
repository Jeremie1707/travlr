<div class=" d-flex content-comment">
  <div class="comment-avatar">
    <%= cl_image_tag current_user.photo, class: "avatar" %>
  </div>
  <%= simple_form_for([trip, trip_item, comment], remote: true, html: {class: "form-inline flex-grow-1"} )do |form| %>
  <!-- <div class="row "> -->
    <%= form.input :comment, as: :text, label: false, input_html: {class: 'comment-box flex-grow-1'}, placeholder: "What do you think...?" %>

    <%= form.button :submit, value:"send", class:"btn btn-success" %>
  <!-- </div> -->
  <% end %>
</div>

