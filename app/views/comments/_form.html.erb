  <div class="row justify-content-center">
  <%= simple_form_for([ trip_item, comment ], remote: true) do |form| %>
    <%= form.input :comment, as: :text, placeholder: "What do you think?" %>
    <div class="row justify-content-center">
    <%= form.button :submit %>
    </div>
  <% end %>
  </div>
